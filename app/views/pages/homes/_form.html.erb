<%= form_with(model: pages_home, local: true) do |form| %>
  <% if pages_home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pages_home.errors.count, "error") %> prohibited this pages_home from being saved:</h2>

      <ul>
        <% pages_home.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
